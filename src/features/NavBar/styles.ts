import { styled } from 'styled-components'

import { View } from 'react-native'

export const SpotifyButton = styled(View)`
  position: absolute;
  top: -16px;
  align-items: center;
  justify-content: center;
  width: 64px;
  height: 64px;
  background-color: black;
  border-radius: 50px;
`
