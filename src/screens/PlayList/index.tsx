import { Text, View } from 'react-native'

const PlayList = () => {
  return (
    <View style={{ backgroundColor: 'black', flex: 1 }}>
      <Text>PlayList</Text>
    </View>
  )
}

export default PlayList
